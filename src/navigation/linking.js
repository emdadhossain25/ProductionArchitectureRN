const config = {
  screens: {
    HomeTab: 'home',
    ProfileTab: 'profile',
    SettingsTab: 'settings',
  },
};

const linking = {
  prefixes: ['productionarch://'],
  config,
};

export default linking;
